services:
  watchparty-v6:
    image: muaeabudhabi/watchparty-v6:latest
    restart: unless-stopped
    labels:
      com.centurylinklabs.watchtower.enable: 'true'
      org.titan.app: watchparty-v6
    environment:
    - NODE_ENV=production
    - LOG_LEVEL=info
    - PUID=99
    - PGID=100
    - UMASK=022
    - DATABASE_URL=postgresql://watchparty-v6:changeme@watchparty-v6_postgres:5432/watchparty-v6
    - REDIS_URL=redis://watchparty-v6_redis:6379
    volumes:
    - /mnt/user/appdata/watchparty-v6/data:/data
    - /mnt/user/appdata/watchparty-v6/config:/config
    logging:
      driver: json-file
      options: &id001
        max-size: 10m
        max-file: '3'
    networks:
    - watchparty-v6_net
    depends_on:
      watchparty-v6_postgres:
        condition: service_healthy
      watchparty-v6_redis:
        condition: service_healthy
  watchparty-v6_postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
    - POSTGRES_DB=watchparty-v6
    - POSTGRES_USER=watchparty-v6
    - POSTGRES_PASSWORD=changeme
    volumes:
    - watchparty-v6_postgres_data:/var/lib/postgresql/data
    networks:
    - watchparty-v6_net
    logging:
      driver: json-file
      options: *id001
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER:-app}
      interval: 30s
      timeout: 5s
      retries: 5
  watchparty-v6_redis:
    image: redis:7-alpine
    restart: unless-stopped
    environment: []
    volumes:
    - watchparty-v6_redis_data:/data
    networks:
    - watchparty-v6_net
    logging:
      driver: json-file
      options: *id001
    healthcheck:
      test:
      - CMD
      - redis-cli
      - ping
      interval: 30s
      timeout: 5s
      retries: 5
networks:
  watchparty-v6_net:
    driver: bridge
volumes:
  watchparty-v6_data:
    driver: local
  watchparty-v6_postgres_data:
    driver: local
  watchparty-v6_redis_data:
    driver: local
